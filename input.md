Please fix the dates page.

Remove search by name.

In the filter remove Visited and Want to go (only from the filter)

Also change the order of things, for the page i want

first filter
then list of locations for dates and last i want the built in map

Also fix the marker in the map. currently it is not loading the image of the marker

Also when you add date. Location autocomplete is not working for location field.

Try with this:
I use leaflet.js to create the map and OSM as the service provider. To add an autocomplete search box I use Leaflet Geoapify Address Search, which is powered by Geoapify powers.

Also from add date remove dates and add a checkbox with the text "Favourite" so you can add it to favourite