Update the following:

(needs testing) 1. Please change the dashboard id format used by the web app
Currently it has this format: da9a4de3-e3bd-432e-a201-1f2e88613be3
Format i want: 1234-5678
Expected output: i want it to be four random numbers followed by a dash and then four more numbers.


(needs testing) 2. There is a problem with the UI for the buttons when you over with the mouse.
Currently when i over some buttons it has some strange color. It happens with the orange box, when i over with the mouse it turns to a very ugly greyish orange and you are not able to read the text in the button. Some buttons i saw that have this are "Add activity", "add task", "add location", "add trip", "add recipe", "add tv show", "add movie", "add book", "exit dashboard", "create dashboard", "join dashboard", "register", "create account". "sign in" (there can have more buttons like this)


(needs testing) 3. Please updated the account button used in the header (this button appears in multiple pages).
Currently it is liek this: the account button has a logo of the user and "Account" and it is highlighted.
Expected output: i except the Account button (this button appears both oh private dashboard and dashboard selector) to be change to just be the icon of a person and the text "Account" and no longer highlighted


(needs testing) 4. When you are in the private page, you shouldnt be able to click the logo/ couple planner/ dashboard name to take you back to dashboard selection (this is present in the header of the private dashboard page). it should just be a logo with the app name and the dashboard name bellow (as it is right now). So what i except for output if for you to not change anything in the header except the ability to click on the logo/app name/ room name to take you back to dashboard selection page.

(needs testing) 5. Update the privacy policy and terms of service page. the width of the box with the text should be more wider. also update the header it should show login/ register if you are not logged in and if you are loged in if should show you "Go to dashboard" that takes you to your private dashboard and "Account" button (account button should be the same that is used in dashboard selection and private dashboard)

(needs testing) 6. Update the header of the homepage. it should show login/ register if you are not logged in and if you are loged in if should show you "Go to dashboard" that takes you to your private dashboard and "Account" button (account button should be the same that is used in dashboard selection and private dashboard)