For the shelf page (shelf that is unique to a group of users). this should be the header

To the left
1. Shelf name
2. <icon> Shelf items (on the left of each item there should be a corresponding icon)

To the right
1. <icon> Settings
2. <icon> Profile

Remove the add from header, there should be an add button on the body of the page for each shelf item.
Remove logout button, it is already in profile popup
also remove back button from header and add it instead to progilr popup. Now in profile it should have name, username, email and  buttons (edit information, back and logout, in this order. And you can put the buttons in vertical line, meaning one on top of each other)

Also pelease remove the subheader that has the online status. Also it has tvshows moveis and books on watchlist, that needs to be removed too from the header.
Instead, when yo uclick watcvhlist the screen should be 3 squares - tvshows, movies and books. And after clicking it is when you are taken to the watchlist of that specific media content.